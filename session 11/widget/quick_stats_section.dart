/*
import 'package:flutter/material.dart';
import 'package:home_work_project/widget/card_state_widget.dart';

class QuickStatsSection extends StatelessWidget {
  const QuickStatsSection({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text('Quick Stats',style: TextStyle(
          fontSize: 16,
          fontWeight: FontWeight.bold,
        ),),
        SizedBox(height: 16,),
        Row(
          mainAxisAlignment: MainAxisAlignment.spaceAround,
          children: [
            CardStateWidget(colorIconState: Colors.deepPurple,iconState: Icons.people,numberState: '1,234',textState: 'Users',),
            CardStateWidget(colorIconState: Colors.orange,iconState: Icons.star_sharp,numberState: '4.8',textState: 'Rating',),
            CardStateWidget(colorIconState: Colors.lightBlueAccent,iconState: Icons.timeline,numberState: '98%',textState: 'Success',),
          ],
        )



      ],
    );
  }
}
 */